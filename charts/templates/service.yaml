apiVersion: v1
kind: Service
metadata:
  name: {{ include "sugar-suite.fullname" . }}
  labels:
    {{- include "sugar-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sugar-suite.selectorLabels" . | nindent 4 }}
